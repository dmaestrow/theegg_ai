%TAREA21 Fraccion irreducible para x ={0,0001 y 0,9999} (4 cifras decimales)
%   Un programa que dado un numero introducido entre 0,0001 y 0,9999 (no más de 4 cifras decimales), obtenga y muestre la correspondiente fraccion irreducible.
clc;close all;clear;


num_digits = 4;

% input: random variable from uniform distribution [0,1], rounded to
% num_digits decimals
x = rand(1);
x = round(x,num_digits);

% compute and show result
[a,b] = get_irreducible_fraction(x);
fprintf([num2str(a),'/',num2str(a)]);

function [numerator,denominator]= get_irreducible_fraction(x,num_digits)

%     initialize fraction
    numerator = x;
    denominator = 10^num_digits;
    
    while(rem(numerator,b) 
    
    
                 
    
%     máximo común divisor- MCD
    

end